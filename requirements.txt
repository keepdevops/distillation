# Core ML stack — hardware-specific installs handled by scripts/install.sh
torch>=2.0
torchvision
torchaudio

# NLP / training
transformers>=4.38
datasets>=2.14
accelerate>=0.24
peft>=0.9
evaluate>=0.4
trl>=0.10

# UI
gradio>=6.0

# Utilities
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.0
rich>=13.0
psutil>=5.0

# Apple Silicon only (installed by scripts/install.sh on osx-arm64)
# mlx
# mlx-lm>=0.20
# coremltools>=8.0

# NVIDIA only (installed by scripts/install.sh when nvidia-smi detected)
# bitsandbytes

# Optional
# unsloth[mlx]
